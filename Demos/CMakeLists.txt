add_subdirectory(SampleApp)
if(CEF3D_BUILD_DIRECT3D11_SAMPLE)
    add_subdirectory(BasicApp_OSR_Direct3D11)
endif()
if(CEF3D_BUILD_DIRECT3D12_SAMPLE)
    #add_subdirectory(OSR_Direct3D12)
endif()

add_subdirectory(MMD_Profile)
add_subdirectory(MMD_Nowplaying)
add_subdirectory(MMD_Rym)